/*
[1] keydown => When Click On Key

[2] keypress => The keypress event is fired when a keyboard key is pressed,
but this event only deals with keys that produce a character.
If a key is pressed that does not result in a character (such as Shift or Ctrl),
this event will not be fired.

[3] keyup => When the finger is lifted from the key.
*/
